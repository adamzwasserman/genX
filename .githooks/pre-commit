#!/bin/bash
# genx.software Pre-commit Hook
#
# Cache purging is no longer needed - CDN assets use content-hash
# filenames for automatic cache busting. HTML files reference the
# hashed filenames, so browsers automatically fetch new versions.
#
# Example: fmtx.8235482c.min.js - hash changes when content changes

exit 0
